{
  "hooks": {
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Save current flow state before compaction using the flow-state.sh script:\n\nSCRIPT=\"${FLOW_PLUGIN_ROOT}/skills/shared/scripts/flow-state.sh\"\n\n1. Read current state: bash \"$SCRIPT\" get\n2. If state is empty ({}), check if there is active context to save:\n   - If autonomous mode is running or beads issues are active, run: bash \"$SCRIPT\" init\n   - Then set fields as needed with: bash \"$SCRIPT\" set key=value\n3. If state already exists, update it with current context:\n   - bash \"$SCRIPT\" set beads_issue_id=<id_or_null>\n   - Preserve current_phase and prd_summary (these are critical for context reset after PRD approval)\n4. Read prd-context.json if it exists and include prd_context:\n   - bash \"$SCRIPT\" get prd_context\n   - If empty, check prd-context.json: cat \"$(git rev-parse --show-toplevel 2>/dev/null)/.flow/state/prd-context.json\" 2>/dev/null\n   - If found, set it: bash \"$SCRIPT\" set 'prd_context={...}'\n5. Call /flow:summary to preserve context if autonomous mode is active",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|compact|clear|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/export-plugin-root.sh",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Restore flow state after compaction using the flow-state.sh script:\n\nSCRIPT=\"${FLOW_PLUGIN_ROOT}/skills/shared/scripts/flow-state.sh\"\n\n1. Read state: bash \"$SCRIPT\" get\n2. Check current_phase: bash \"$SCRIPT\" get current_phase\n3. If current_phase == 'approved':\n   - Read prd_summary: bash \"$SCRIPT\" get prd_summary\n   - Display PRD approval summary with feature_name, version, branch, requirements_count, prd_path\n   - Check mode: bash \"$SCRIPT\" get mode\n   - If mode == 'autonomous': Auto-invoke /flow:generate-tasks to continue autonomous execution\n   - If mode == 'manual': Suggest: \"Run /flow:generate-tasks to create implementation tasks from the approved PRD.\"\n4. If mode == 'autonomous' (non-approved phase):\n   - Notify user: \"Autonomous session was interrupted by compaction. Session restored from checkpoint.\"\n   - Suggest: \"Run /flow:summary for full context refresh\"\n5. Check beads_issue_id: bash \"$SCRIPT\" get beads_issue_id\n   - If set: Notify user: \"Beads issue tracking preserved. Issue ID: {beads_issue_id}\"\n6. Continue workflow based on restored state",
            "timeout": 30
          }
        ]
      },
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check for pending flow state from a previous session using the flow-state.sh script:\n\nSCRIPT=\"${FLOW_PLUGIN_ROOT}/skills/shared/scripts/flow-state.sh\"\n\n1. Read state: bash \"$SCRIPT\" get\n2. If state is not empty ({}) and current_phase == 'approved':\n   - Read prd_summary: bash \"$SCRIPT\" get prd_summary\n   - Inform user: \"A previously approved PRD was found from a prior session.\"\n   - Display: feature_name, version, branch from prd_summary\n   - Suggest: \"Run /flow:generate-tasks to generate implementation tasks, or /flow:plan to start a new plan.\"\n3. If state is empty or current_phase is not 'approved': do nothing (silent)",
            "timeout": 15
          }
        ]
      },
      {
        "matcher": "startup|compact|clear|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/discover-prd-context.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/verify-completion.sh",
            "timeout": 30
          }
        ]
      },
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/persist-state.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
